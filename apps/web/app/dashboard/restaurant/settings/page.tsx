import { RestaurantSettingsAdmin } from "@/components/commerce-admin";
export const metadata = { title: "Restaurant settings" };
export default function RestaurantSettingsPage() { return <RestaurantSettingsAdmin />; }
